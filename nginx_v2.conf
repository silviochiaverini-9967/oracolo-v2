# Da includere nel server block esistente di oracolo.bet.
# Il v2 vive su /v2/ nello stesso dominio: same-origin, niente CORS.
location /v2/ {
    alias /var/www/oracolo_v2/;
    try_files $uri $uri/ /v2/index.html;
}
